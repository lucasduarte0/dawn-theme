<div class="row">
  <div class="page-header">
    {% if current_tags %}    
      <h1><a href="{{ blog.url }}">{% render 'multilang' with page_title %}</a> &raquo; {{ current_tags.first }}</h1>
    {% else %}    
      <h1>{% render 'multilang' with page_title %}</h1>
    {% endif %}
  </div>


  {% section 'blog-template' %}  
</div>